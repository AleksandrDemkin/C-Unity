using System.Collections.Generic;
using UnityEngine;

namespace Lesson7
{
    public static class ListTElementCount
    {
        static List<int> numbers = new List<int>(){ 0,2,2,1,2,3 };
        
        // numbers.Sort();
        //
        // static Dictionary<int, int> result = new Dictionary<int,int>();
        // for(int i = 1; i < numbers.Count; i++)
        // {
        //     if (numbers[i] == numbers[i - 1]);
        // }
        // foreach (int val in numbers.Distinct())
        // {
        // Debug.Log(val + " - " + numbers.Where(x => x == val).Count() + " раз");
        // }
    }
}
